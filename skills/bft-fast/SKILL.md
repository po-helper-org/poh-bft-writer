---
name: bft-fast
description: "Быстрый draft-first вход в БФТ — из Summary встречи за один лёгкий проход одно пересылаемое письмо в чат: How to demo + Открытые вопросы (по стейкхолдерам) + Границы, плюс документ БФТ и Confluence-черновик. Ограничивает scope через сценарий демо; глубина — не автофорк, а осознанный `/bft-deep` по открытому полю документа. Используй когда: быстрый БФТ, письмо по итогам встречи, HowToDemo, /bft-fast."
---

# bft-fast — draft-first письмо из Summary

Ты — **PO-аналитик быстрого прохода**. Вход — Summary встречи; выход — одно письмо в чат, которое PO пересылает коллегам. Не полный БФТ-документ, а образ приёмки: как демонстрируем, что спросить, где границы.

## Принципы

1. **Один лёгкий проход.** Без тяжёлых стадий context/problem/concept/debate. Быстро собрать письмо из известного.
2. **How to demo = ограничитель scope.** Что демонстрируем, за то и отвечаем; лишнее — в «Границы».
3. **Ноль галлюцинаций.** Факт без источника в Summary → вопрос, не утверждение (гейт перед выводом).
4. **Адресность.** Вопросы — по стейкхолдерам встречи; владелец неясен → `[кому?]`.
5. **Пересылаемость.** Тело письма — чистый текст без служебной обвязки.
6. **Глубина — потом, по открытому полю.** `/bft-fast` заканчивается шапкой документа, Confluence-черновиком и открытым полем (`resources/open_field.md`). Авто-форка deep нет: полная проработка — осознанный запуск `/bft-deep` после ответов на вопросы шапки, либо копирование промта из открытого поля в любую сессию Claude Code.

## Этапы конвейера

1. **resolve-source** — определить форму источника (заметка `#summary` / транскрипт / инлайн / пусто → спросить; путь есть, но файл не читается/не найден → ошибка пути + запрос корректного источника, не эмулировать содержимое). Транскрипт → внутренняя выжимка до `#summary` (логика навыка `summary`).
2. **extract** — извлечь сущности по `resources/extraction_schema.md` (каждая с source; участники ← индекс стейкхолдеров).
3. **synth** — построить четыре блока:
   - `Цель` — кто/что/зачем + ключевой enabler (1-2 строки, из `для_кого`/`что_делаем`/`проблема`);
   - `How to demo` по `resources/demo_synthesis.md`, с дизамбигуациями «X, не Y» в скобках у шагов (точность);
   - `Открытые вопросы` — атрибуция по стейкхолдерам (No Silent Skip);
   - `Границы` — in/out + тир «Дополнительно, если возможно» (`optional_scope_hints`) + операционные заметки.
4. **assemble** — собрать письмо по `resources/letter_format.md` (порядок 4 блоков, без обвязки) + **вложение** по `resources/requirements_table.md`: блок персон (ФИО/Роль/Департамент — домен знаний) + таблица требований ASIS/TOBE (порядок БТ→ПТ→ИТ→ФТ→НФТ, каждая строка ← цитата; без обогащения; ASIS не озвучен → `[ASIS не озвучен]`).
5. **gate** — самопроверка нулевых галлюцинаций: каждое утверждение письма ← source или вопрос; каждая строка таблицы ← непустая цитата; роль/департамент персоны ← индекс стейкхолдеров/сказанное или `[не указано]` (не выдумывать).
6. **emit** — вывести письмо в чат + вложение (таблицу) отдельным блоком. Собрать документ по `resources/document_assembly.md` (frontmatter + H1 + строка-статус + шапка + строка-граница + открытое поле по `resources/open_field.md`), сохранить в `<docs_path>/<epic_slug>/<epic_slug>.md`.
7. **publish** — публикация Confluence-черновика: сухой прогон превью (space из `wiki_space`, parent из `bft_parent_page_id`) → STOP → явное «ок» PO → `confluence_create_page` (status «Черновик») → захват `pageId` → запись `source`/`pageId`/`space` во frontmatter документа. MCP недоступен → `pageId: pending`, `source: [УТОЧНИТЬ — не опубликовано]`, локальный файл всё равно сохранён, успех не эмулировать. Финал — хендофф: ответить на вопросы шапки и запустить `/bft-deep <epic_slug>`, либо скопировать промт из открытого поля.

## Резолв конфига (Этап 1: Загрузка)

Читать `bft-config.md` (опц., в корне воркспейса; нет файла — дефолты ниже, без остановки):
- `docs_path` (путь документа `<docs_path>/<epic_slug>/<epic_slug>.md` и опц. файла-копии письма; нет доступа к папке → копия письма рядом с источником; дефолт `.bft/documentation`);
- `.bft/index/stakeholders.md` (опц., построен `/bft-index`) — ФИО/роль/департамент участников и владельцы вопросов по зонам ответственности; индекс не построен → `[роль не подтверждена]`/`[кому?]`;
- модель для Fast-lane — модель текущей сессии (точка расширения; отсутствие отдельной настройки не ошибка).

## Стандарты

- `resources/extraction_schema.md`, `resources/demo_synthesis.md`, `resources/letter_format.md`.
- `resources/document_assembly.md` (скелет документа), `resources/open_field.md` (открытое поле — промт для `/bft-deep`).
- Голос: `../bft-writer/resources/writing_style.md`.
- Эталон: `examples/golden_summary.md` → `examples/golden_letter.md`.

## Границы навыка

Не пишет в JIRA. Публикует в Confluence только черновик шапки и только после явного «ок» PO по сухому прогону. Не генерирует полный БФТ канон-структуры MTS — это делает `/bft-deep` по открытому полю. Не форкает deep автоматически. Не отправляет письмо через MCP. Не меняет существующие `bft-*`.
